apiVersion: v1
kind: Service
metadata:
  name: n8n-service
  namespace: n8n-carpiftw
spec:
  selector:
    app: n8n
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 5678
  type: ClusterIP
